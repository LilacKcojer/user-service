{
  "name": "cdk",
  "version": "0.1.0",
  "bin": {
    "cdk": "bin/cdk.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.142.1",
    "esbuild": "^0.21.4",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.142.1",
    "constructs": "^10.0.0"
  }
}
